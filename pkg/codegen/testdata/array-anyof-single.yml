openapi: "3.0.0"
info:
  version: 1.0.0
  title: Array with single anyOf
paths:
  /test:
    get:
      operationId: getTest
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  issues:
                    type: array
                    items:
                      anyOf:
                        - type: object
                          properties:
                            issue:
                              type: string
                            description:
                              type: string

